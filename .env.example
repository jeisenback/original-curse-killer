# Copy to .env and fill values
DISCORD_BOT_TOKEN=
DISCORD_ALLOWED_USERS= # comma-separated numeric IDs
ROUTES_FILE=routes.json
# Optional: full path to the claude CLI binary (defaults to "claude" on PATH)
CLAUDE_CLI_PATH=
