# Copy to .env and fill values — never commit .env

# Discord bot token (required)
DISCORD_BOT_TOKEN=

# Comma-separated Discord user IDs allowed to send messages; leave empty for everyone
DISCORD_ALLOWED_USERS=
